export const environment = {
    production: true,
    baseUrl: "http://backend:8080/api",
    keycloak: {
        baseurl: "http://keycloak:8081",
        realm: "taurus",
        clientId: "web-app",
    }
};